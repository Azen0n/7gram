services:
  bot:
    container_name: sevengram_bot
    build: .
    volumes:
      - ./sevengram:/opt/sevengram/app/sevengram

    # Include development dependencies when working locally
    environment:
      - UV_NO_DEV=0
